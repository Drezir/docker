# has to be next to .git folder
sudo: required
services:
  - docker

before_install:
  - docker build -t adam/docker -f flow/Dockerfile.dev .

# do not prompt for user input after end of tests
script:
  - docker run adam/docker npm test -- --coverage